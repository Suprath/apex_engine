# Performance microbenchmarks for Apex Engine
# Benchmark infrastructure and placeholder for benchmark targets

# Benchmark executables validate latency and throughput targets
# Add benchmarks as features are implemented

# Example benchmark target (uncomment when benchmark files are added):
# add_executable(bench_throughput bench_throughput.cpp)
# target_include_directories(bench_throughput PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../include)
# target_link_libraries(bench_throughput PRIVATE apex)
# target_compile_options(bench_throughput PRIVATE ${APEX_COMPILE_FLAGS})

message(STATUS "Benchmarks directory: add benchmark executables as performance-critical features are implemented")
